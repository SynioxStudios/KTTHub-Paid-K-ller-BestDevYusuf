--[[
    🚀 YUSUF EMIR REDYUS - LOADER v2 🚀
--]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local whitelistUrl = "https://pastebin.com/raw/bj3aQnNh" 
local scriptUrl = "https://raw.githubusercontent.com/SynioxStudios/SynioxKillSC-PA-DHUB-YusufDev-AutoKill/refs/heads/main/Synioxkill.txt" 

local function DogrulamaVeCalistir()
    local success, content = pcall(function()
        return game:HttpGet(whitelistUrl)
    end)

    if success then
        local loadSuccess, whitelist = pcall(function()
            return loadstring(content)()
        end)

        if loadSuccess and type(whitelist) == "table" then
            local myName = LocalPlayer.Name:lower()
            local yetkiVarmi = false

            for name, value in pairs(whitelist) do
                if name:lower() == myName and value == true then
                    yetkiVarmi = true
                    break
                end
            end

            if yetkiVarmi then
                warn("✅ ERİŞİM ONAYLANDI!")
                loadstring(game:HttpGet(scriptUrl))()
            else
                -- Mesajı daha kısa ve net yaptık ki ekranda görünsün
                LocalPlayer:Kick("\n\n🚫 [YETKİSİZ ERİŞİM] 🚫\n\nİsminiz listede yok!\nSahip: yusufemirredyus")
            end
        else
            LocalPlayer:Kick("❌ Liste Okunma Hatası!")
        end
    else
        LocalPlayer:Kick("❌ İnternet/Bağlantı Hatası!")
    end
end

DogrulamaVeCalistir()
